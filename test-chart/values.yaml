# Base values - defaults for all test patterns
# Each test-values/*.yaml overrides specific sections

ifSimple:
  enabled: false

ifElse:
  useOptionA: false

ifElseIf:
  tier: "bronze"

inlineIf:
  custom: ""

inlineIfElse:
  custom: ""

inlineChain:
  level: "low"

nestedIf:
  level1: false
  level2: false
  level3: false

withBlock: {}

rangeTest:
  items: []

multiInline:
  a: false
  b: false
