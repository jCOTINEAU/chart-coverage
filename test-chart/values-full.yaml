replicaCount: 3

image:
  repository: nginx
  tag: "1.21"

ingress:
  enabled: true
  host: example.com

service:
  enabled: true
  type: ClusterIP
  port: 80

resources:
  enabled: true
  limits:
    cpu: 100m
    memory: 128Mi
